package cartographer.ui.workstation;

/** Stable Workstation tool identity shared by navigation, options and controller routing. */
public enum WorkstationTool {
    MAP,
    COVERAGE,
    ORE,
    SURFACE,
    GEOLOGY,
    PROSPECTING
}
