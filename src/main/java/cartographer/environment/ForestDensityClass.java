package cartographer.environment;

public enum ForestDensityClass {
    /*
     * Cartographer-derived density buckets over normalized ForestMap values,
     * not official Vintage Story biome classes.
     */
    NONE,
    SPARSE,
    LIGHT,
    MODERATE,
    DENSE,
    VERY_DENSE
}
