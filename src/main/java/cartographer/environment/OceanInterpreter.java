package cartographer.environment;

public class OceanInterpreter {
    public OceanSample interpret(
            int rawValue
    ) {
        return new OceanSample(
                rawValue
        );
    }
}
